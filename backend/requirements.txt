# --- Framework ---------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0

# --- ORM / DB ----------------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10        # usado por Alembic en modo sync

# --- Validación / Settings ---------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Seguridad ---------------------------------------------------------------
argon2-cffi==23.1.0
pyjwt==2.10.1
python-multipart==0.0.20
itsdangerous==2.2.0            # firma de tokens CSRF / cookies

# --- Cache / Broker ----------------------------------------------------------
redis==5.2.1
celery==5.4.0

# --- Almacenamiento ----------------------------------------------------------
boto3==1.35.92                 # cliente S3 compatible con MinIO
botocore==1.35.92

# --- QR ----------------------------------------------------------------------
qrcode[pil]==8.0
segno==1.6.1                   # QR vectorial (SVG) de alta calidad
Pillow==11.0.0

# --- Utilidades --------------------------------------------------------------
httpx==0.28.1
tenacity==9.0.0
python-slugify==8.0.4
structlog==24.4.0
user-agents==2.2.0            # device tracking
